# 021-社会工程学实践复现方案

本题属于社工信息恢复与哈希校验题，复现重点不是网络攻击过程，而是如何把公开线索压缩成可验证的身份证候选集合。

## 一、复现目标

复现时应证明：根据生日图片、车辆归属地、题目文本与 `userinfo.xlsx` 中戈德海对应的脱敏尾号和 MD5，可以唯一恢复完整身份证号 `511421198307282710`。

## 二、最小复现材料

| 材料 | 用途 |
| --- | --- |
| `题目信息.txt` | 提供 7 月 28 日生日线索 |
| `birthday.jpg` | 佐证生日日期 |
| `car.jpg` | 提供地域线索 `川Z` |
| `userinfo.xlsx` | 提供身份证末两位 `10` 与 MD5 |
| 身份证校验位规则 | 用于构造合法身份证号 |

## 三、建议复现步骤

首先从生日图片和题面中确认目标人物的生日为 7 月 28 日；随后从车辆照片中识别 `川Z` 车牌，并确认其归属地为四川省眉山市；再从 Excel 中读取戈德海对应的身份证尾号 `10` 与 MD5 值；然后对眉山市候选地址码、出生年份和三位顺序码进行枚举，按身份证校验位规则生成完整号码；最后比对其 MD5，仅保留唯一命中的结果。

## 四、预期复现结论

若输入线索与题目附件一致，应稳定恢复出以下结果：

```text
flag{511421198307282710}
```
