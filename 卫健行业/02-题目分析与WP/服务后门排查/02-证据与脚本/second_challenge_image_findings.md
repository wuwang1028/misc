# 第二道题截图识别结果

从用户提供的截图可直接识别出，本题的正式题名为 **服务后门排查**。

题面文字显示：某公司服务器被攻击者写入了定时任务，定时执行恶意行为，请协助助处置该事件（相关见威内容可直接清除），处置完成后读取根目录下的 flag 并提交。备注说明为：请用 http 访问端口，账号口令均为 `ctf`。

截图中的私有场景还给出了交换机网络信息，包含以下参数。

| 项目 | 取值 |
| --- | --- |
| 网络配置 | `192.168.1.0/24` |
| 网关 | `192.168.1.254` |
| vlanID | `1001` |

结合当前新实例中的排查结果，可以确认本题与 **恶意计划任务 / 定时任务后门清理** 直接相关，归档时应按“服务后门排查”题名组织材料。

进一步查看第二张截图后，可补充确认如下信息。

| 项目 | 取值 |
| --- | --- |
| 题目名称 | `服务后门排查` |
| 主机节点名 | `2024dxtyy02` |
| 内网地址 | `192.168.1.222` |
| 访问环境 | `控制台 192.168.1.222` |
| 原始 http 入口示例 | `http://10.30.15.17:45409` |

这说明题目最初就是通过 Web 终端方式让选手登录目标主机，在主机内完成定时任务后门排查与清理。
