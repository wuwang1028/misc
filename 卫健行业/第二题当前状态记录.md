# 第二道题当前状态记录

- 目标靶场：`http://oj-10-30-15-17-45409.adworld.xctf.org.cn/`
- 2026-04-15 当前浏览器访问结果：页面直接返回 **502 Bad Gateway**，服务端标识为 `openresty`。
- 同步使用已有 WebSocket 脚本连接 `ws://oj-10-30-15-17-45409.adworld.xctf.org.cn/ws` 时，也出现 `Handshake status 502 Bad Gateway`。
- 说明：当前靶场实例暂时不可用，无法继续进入 ttyd 终端完成取证与取旗。
- 已知此前在靶场正常时完成过多轮排查：
  - 已将 `/home/ctf/.ash_history` 处理为指向 `/dev/null` 的符号链接；
  - 已确认 `ctf` 用户 crontab 当前为空；
  - 等待判题约 95 秒后仍未出现 `/flag`；
  - 当前阶段需要优先等待靶场恢复，再继续验证是否仍存在遗漏项。
